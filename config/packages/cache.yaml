framework:
    cache:
        # Dedicated pool for the dashboard read model.
        # Using a separate pool means clear() in SiteMetricProjector only
        # affects dashboard entries and nothing else in the app cache.
        # Switch the adapter to Redis or APCu for production.
        pools:
            app.cache.dashboard:
                adapter: cache.adapter.filesystem
